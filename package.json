{
  "name": "shopify-automation-app",
  "version": "1.0.0",
  "description": "Shopify webhook automation — Abandoned Checkout, Welcome Emails, Low Stock Alerts",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test:webhook": "node scripts/test-webhook.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "mongodb": "^6.13.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  },
  "engines": {
    "node": ">=20.x"
  }
}
